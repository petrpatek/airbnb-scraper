const MAX_PRICE = 50000; // Default is in currency
const MIN_PRICE = 0;
const HISTOGRAM_ITEMS_COUNT = 100;
const LOW_LIMIT = 10;
const MAX_LIMIT = 50;

module.exports = {
    LOW_LIMIT,
    MAX_LIMIT,
    MAX_PRICE,
    MIN_PRICE,
    HISTOGRAM_ITEMS_COUNT,
};
